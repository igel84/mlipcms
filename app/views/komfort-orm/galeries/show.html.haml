:css
  a.fancybox img { margin-right: 30px; margin-bottom: 30px; }
- @galery.photos.each do |photo|
  = link_to image_tag(photo.image.small), photo.image.url, class: 'fancybox', title: photo.name, rel: 'galery'